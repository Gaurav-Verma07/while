import { Herobox } from '@/components';
import Head from 'next/head';

const index = () => {
  return (
    <div>
      <Head>
        <title>While</title>
      </Head>
      <Herobox />
    </div>
  );
};

export default index;
